[tox]
envlist = py{34,35}

[testenv]
deps = pytest
       pytest-doctest-custom
       functional
       ipython
commands = py.test

[pytest]
addopts = --doctest-modules
          --doctest-glob=*.rst
          --doctest-repr=IPython.lib.pretty:pretty
          --ignore setup.py
